# main providers libraries. This can be generated by running terraform init, provided the terraform.lock.hcl file is present.
.terraform/*

# the terraform state will also be ignored because these files are generated by terraform and they change often.
*.tfstate*

# the terraform tfvars will also not be pushed because this might containe sensitive information
terraform.tfvars